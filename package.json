{
  "name": "portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && npx gh-pages -d dist"
  },
  "dependencies": {
    "bootstrap": "^5.3.3",
    "swiper": "^11.1.4"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^5.2.0"
  }
}
